"""Inline HTML/JS/CSS page constants extracted from bluetools.py.

Each module holds the verbatim string constants for one page group.
These modules must not import bluetools (they are pure data).
"""
