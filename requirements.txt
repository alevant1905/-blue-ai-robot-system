# Blue Enhanced - Python Requirements
# Install all at once with: pip install -r requirements.txt

# Core Requirements (Required)
flask>=2.3.0
flask-cors>=4.0.0
requests>=2.31.0
werkzeug>=2.3.0

# Database (Built-in with Python)
# sqlite3 comes with Python, no installation needed

# Enhanced Features (Recommended)
pillow>=10.0.0              # Screenshots and image processing
psutil>=5.9.0               # System monitoring (CPU, RAM, disk)

# Gmail Integration (Optional - for email features)
google-auth>=2.22.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.95.0

# Music Integration (Optional - for YouTube Music)
ytmusicapi>=1.3.0

# Smart Home Integration (Optional - for Philips Hue)
# No additional packages needed - uses direct HTTP requests

# Document RAG System (for semantic PDF search)
chromadb>=0.5.0             # Vector database with built-in embeddings (>=0.5 for NumPy 2.x compat)
pypdf>=4.0.0                # PDF text extraction (maintained successor to PyPDF2)
python-docx>=1.0.0          # Word .doc/.docx text extraction
openpyxl>=3.1.0             # Excel .xlsx text extraction
python-pptx>=0.6.23         # PowerPoint .pptx text extraction
striprtf>=0.0.26            # Rich Text Format extraction
watchdog>=3.0.0             # Filesystem watcher for auto-indexing dropped documents

# Advanced Features (Optional)
python-dateutil>=2.8.2      # Better date/time parsing
pytz>=2023.3                # Timezone support

# Clipboard & Notifications (Optional - for system tools)
pyperclip>=1.8.2            # Cross-platform clipboard access
plyer>=2.1.0                # Desktop notifications

# Volume Control - Windows Only (Recommended for set_volume tool)
# Without these, set_volume falls back to a SendKeys-based PowerShell hack
# that is slower and steps in 2% increments.
pycaw>=20220416              # Windows audio control
comtypes>=1.2.0              # Required by pycaw

# Web Scraping (Optional - for web tools)
beautifulsoup4>=4.12.0      # HTML parsing
ddgs>=5.0.0                 # DuckDuckGo search

# Media Control (Optional)
pyautogui>=0.9.54           # System-wide media keys

# Face Recognition (Recommended - lets Blue recognize who he sees)
# Blue uses OpenCV's built-in YuNet detector + SFace recognizer (no dlib, no
# compilation). opencv-python>=4.5.4 bundles both. The small ONNX model files
# are auto-downloaded from the OpenCV Zoo on first use into data/models/.
opencv-python>=4.5.4        # YuNet (FaceDetectorYN) + SFace (FaceRecognizerSF)
numpy>=1.24.0               # Required for face embeddings
#
# Legacy alternative (NOT used; left for reference). face_recognition needs
# dlib + cmake, which is painful to build on Windows:
# face_recognition>=1.3.0
# dlib>=19.24.0
# cmake>=3.27.0

# Development Tools (Optional - for developers)
pytest>=7.4.0               # Testing
black>=23.7.0               # Code formatting
mypy>=1.5.0                 # Type checking

# Platform-specific (Install if needed)
# Windows: pywin32>=306
# macOS: pyobjc-framework-Cocoa>=9.2
# Linux: python3-gi

# Note: Some system control features may require platform-specific tools:
# Windows:
#   - pycaw for volume control (pip install pycaw comtypes)
#   - pywin32 for clipboard fallback
# macOS: Built-in osascript (already available)
# Linux: amixer for volume control (usually pre-installed)
